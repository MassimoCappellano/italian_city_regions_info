<div class="page-header">
            <h1>Users</h1>
</div>

			<table class="table table-striped">
			{{#if activeSessions}}
				<tr><th>UTENTE</th><th>DATA ACCESSO</th></tr>
					{{#each activeSessions}}
		            	<tr><td>{{this.user}}</td><td>{{this.start_login}}</td></tr>
		            {{/each}}
	            </table>
            {{else}}
            Nessuna sessione attiva
            {{/if}}
